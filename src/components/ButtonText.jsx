const ButtonText = (label) => {
  return (

    <button className="hover:underline-offset-[24px] font-semibold leading-3 tracking-wider text-gray-600 hover:underline hover:text-gray-800">
      {label}
    </button>

  );
};

export default ButtonText;
